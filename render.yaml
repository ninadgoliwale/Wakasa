services:
  - type: web
    name: wakasa-escrow-bot
    runtime: node
    buildCommand: ""
    startCommand: node index.js
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: PORT
        value: 3000
