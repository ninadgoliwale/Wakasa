services:
  - type: web
    name: wakasa-escrow-bot
    runtime: node
    buildCommand: ""
    startCommand: node index.js
    rootDir: .
    envVars:
      - key: BOT_TOKEN
        required: true
      - key: PORT
        value: 3000
